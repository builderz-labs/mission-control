# Plan 04-03: Chat UI Enhancements

## Objective
Enhance existing chat UI with team-aware @mention autocomplete, loop prevention indicators, and shared timeline.

## Context
- Requirements: CHAT-06, CHAT-07, CHAT-08
- Existing: ChatInput with @mention autocomplete in `src/components/chat/chat-input.tsx`
- Existing: ConversationList, MessageList, MessageBubble all functional
- New: Teams API from 04-02, mention routing from 04-01

## Tasks

### Task 1: Enhance @mention autocomplete
Modify `src/components/chat/chat-input.tsx`:
- Include teams in autocomplete dropdown (prefix with "team:" for @team:name)
- Add `@all` and `@human` as special autocomplete suggestions
- Show agent status indicator (online/offline) in autocomplete

### Task 2: Message bubble routing indicator
Modify `src/components/chat/message-bubble.tsx`:
- Show "@agent_name" mentions as highlighted chips in message text
- Show routing status for mention messages (routed, pending, loop prevented)
- Show "Loop prevented" badge when turn limit reached

### Task 3: Shared timeline rendering
Verify `src/components/chat/message-list.tsx`:
- Human and agent messages render in same timeline (CHAT-07)
- Agent auto-responses appear threaded under the original mention
- This should already work since messages share the same conversation_id

### Task 4: Register panel route (if not exists)
Verify `case 'chat':` in ContentRouter routes to ChatPagePanel.

## Wave
Wave 2 (depends on 04-01 + 04-02)
