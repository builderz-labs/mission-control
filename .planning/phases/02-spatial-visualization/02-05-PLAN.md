# Plan 02-05: E2E Specs + Integration Validation

## Objective
Comprehensive E2E tests covering all SPAT requirements and full integration validation.

## Context
- Requirements: SPAT-01 through SPAT-10
- Test runner: Playwright with `pnpm test:e2e:ci` (dot reporter)
- Existing pattern: `tests/*.spec.ts` with API_KEY_HEADER helper

## Tasks

### Task 1: E2E — Spatial API CRUD
Create `tests/spatial-api.spec.ts`:
- POST /api/agents → create 3 agents
- POST /api/spatial/relationships → create delegation + communication edges
- GET /api/spatial/relationships → verify 2 relationships returned
- GET /api/spatial/relationships?agent_id=X → verify filtering
- DELETE /api/spatial/relationships/[id] → verify removal
- PUT /api/spatial/positions → save positions for 3 agents
- GET /api/spatial/positions → verify positions persisted

### Task 2: E2E — Canvas rendering
Create `tests/spatial-canvas.spec.ts`:
- Navigate to `/spatial`
- Wait for React Flow to render (`.react-flow` selector)
- Verify agent nodes appear (`.react-flow__node` count)
- Verify edges appear (`.react-flow__edge` count)
- Verify minimap visible (`.react-flow__minimap`)
- Verify controls visible (`.react-flow__controls`)
- Verify background grid (`.react-flow__background`)

### Task 3: E2E — Node interaction
In `tests/spatial-canvas.spec.ts`:
- Click on agent node → verify detail sidebar appears
- Verify sidebar shows agent name, role, status
- Close sidebar

### Task 4: Full test suite validation
Run complete suite:
```
pnpm typecheck
pnpm test
pnpm test:e2e:ci
```

Verify:
- TypeScript: 0 errors
- Unit tests: 885+ pass (all existing + new spatial tests)
- E2E: All pass including new spatial specs
- No regressions

### Task 5: Update ROADMAP.md progress table
Mark Phase 2 row as complete.

## Verification
- [ ] Spatial API E2E: all CRUD operations tested
- [ ] Canvas rendering E2E: nodes, edges, controls verified
- [ ] Node interaction E2E: click → detail sidebar
- [ ] Full typecheck passes
- [ ] All unit tests pass (0 failures)
- [ ] All E2E tests pass (0 failures)
- [ ] ROADMAP.md updated

## Wave
Wave 3 (parallel with 02-04, depends on 02-03)
