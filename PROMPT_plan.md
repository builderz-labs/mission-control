Study `specs/projects-panel-sidebar.md` to learn the requirements.
Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
Study `src/components/layout/nav-rail.tsx` to understand sidebar structure.
Study `src/components/panels/task-board-panel.tsx` to understand existing panel patterns and TaskDetailModal.
Study `src/store/index.ts` to understand panel registration in Zustand.
Study `src/app/api/projects/` to understand existing project API.

Compare specs against code (gap analysis). Create/update @IMPLEMENTATION_PLAN.md
with a prioritized bullet list of items to implement.

IMPORTANT: Plan only. Do NOT implement anything.
Do NOT assume functionality is missing — search the code first.
Check `src/components/ui/` for existing components before planning new ones.
If specs are missing for a discovered need, create them in specs/.
When plan is ready, add at the bottom: STATUS: PLANNING_COMPLETE
