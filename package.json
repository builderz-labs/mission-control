{
  "name": "mission-control-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Native window for the local Mission Control dashboard",
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "electron .",
    "test": "node --test src/ensure-server.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "electron": "^38.0.0"
  }
}
