# RoceOS Full Stack — Mission Control + LiteLLM + Redis
# Usage: docker compose -f docker-compose.roceos.yml up -d
#
# Phase 0: Access MC directly at http://<vps-ip>:3000
# Future: Add Traefik labels or Caddy for TLS when domain is configured.

services:
  # ── Dashboard / Control Plane ──
  mission-control:
    build: .
    container_name: roceos-dashboard
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NEXT_PUBLIC_GATEWAY_OPTIONAL=true
    env_file:
      - path: .env
        required: false
    volumes:
      - mc-data:/app/.data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - roceos-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Model Proxy (Anthropic-only for now) ──
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: roceos-litellm
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - ./config/litellm.yaml:/app/config.yaml:ro
      - litellm-data:/app/data
    command: ["--config", "/app/config.yaml"]
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - STORE_MODEL_IN_DB=False
    networks:
      - roceos-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Agent Execution Engine (LangGraph + FastAPI) ──
  execution-engine:
    build: ./execution-engine
    container_name: roceos-engine
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - engine-data:/app/data
      - ./agents:/app/agents:ro
    environment:
      - ROCEOS_LITELLM_BASE_URL=http://litellm:4000
      - ROCEOS_MC_API_URL=http://mission-control:3000/api
      - ROCEOS_MC_API_KEY=${MC_API_KEY:-}
      - ROCEOS_TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN:-}
      - ROCEOS_REDIS_URL=redis://redis:6379
    depends_on:
      litellm:
        condition: service_started
    networks:
      - roceos-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Redis (event bus + caching — needed from Phase 3+) ──
  redis:
    image: redis:7-alpine
    container_name: roceos-redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - roceos-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  mc-data:
  litellm-data:
  engine-data:
  redis-data:

networks:
  roceos-net:
    driver: bridge
