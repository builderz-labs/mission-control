# ICT Dashboard API + Signal Broadcast Service
# Copy to .env and fill in values

# Path to trading SQLite database
TRADING_DB_PATH=/opt/trading-workspace/trading/data/trading.db

# JWT secret for agent authentication (generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))")
SIGNAL_JWT_SECRET=
