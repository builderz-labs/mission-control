# Mission Control desktop

Thin Electron shell. The dashboard process stays in `~/Dev/mission-control` on `127.0.0.1:3000`.

- Start server: LaunchAgent `com.tylerdevries.mission-control`
- Open window: `npm start` in this folder
- Tests: `npm test`
- Do not bind off localhost. Do not copy `.env` here.
